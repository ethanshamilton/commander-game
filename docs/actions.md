# Actions

The actions module is the bridge between UI gestures and game intent. It keeps interface code from directly owning gameplay behavior by turning control activation into small, explicit domain actions.
