use bevy::prelude::*;

pub struct MainMenuScenePlugin;

impl Plugin for MainMenuScenePlugin {
    fn build(&self, _app: &mut App) {
        // Stub. Add OnEnter/OnExit systems here when the main menu exists.
    }
}
